// The per-template preview illustrations (spec/09 Quick Start, spec/16):
// one static SVG per TemplateKind, drawn once here and rendered by both
// the editor's template picker and the marketing site's template gallery,
// so a template's card looks the same wherever it is offered and a new
// kind gets its artwork in one place.
export { TemplatePreview } from './template-preview';
