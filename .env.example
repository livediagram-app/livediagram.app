# Required environment variables for local development.
# Copy this file to .env.local in the relevant app/worker and fill in values.
# .env.local is gitignored. Never commit real secrets.
# See specs/06-secrets-policy.md for the full policy.

# (No env vars are required yet — the prototype is frontend-only with localStorage.)
