# Event storming

Follow the references below only as needed; never upfront.

- ./event-storming.md - when working on Event storming: A Technical-category template for the sticky-note domain-exploration workshop, built incrementally: phase 1 ships a near-blank starter (three orange past-tense domain-event stickies over a one-line method reminder). Colour IS the notation, so the stickies pin an explicit orange fill (stickies are theme-exempt); dot-grid backdrop like the other sticky workshop boards. The colour grammar beyond orange (commands, actors, policies, read models, hotspots) lands in later phases per `plans/0003-event-storming.md`
