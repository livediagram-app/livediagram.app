# Event storming

Follow the references below only as needed; never upfront.

- ./event-storming.md - when working on Event storming: the workshop board kind, its note notation, stationery, Alt insertion, timeline lanes (every workshop note always on a lane), next-note buttons and the photo import
- ./blueprints/README.md - when implementing an event-storming spec that has a blueprint (always on a lane)
