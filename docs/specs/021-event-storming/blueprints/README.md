# Event storming blueprints

Follow the references below only as needed; never upfront.

- ./lanes-always.md - when implementing or changing how a workshop note lands on a lane (drag, nudge, paste, duplicate, photo, MCP, settle)
- ./DEFAULTS.md - when an event-storming blueprint applies a default the spec leaves open
- ./COMPLETENESS.md - when checking which completeness categories an event-storming blueprint covers
