# API worker environment variables (docs/specs/002-project-scope/secrets-policy.md).
#
# This worker runs on Cloudflare Workers, not Node. Local dev reads
# these from `apps/api/.dev.vars` (gitignored, see wrangler docs);
# production reads them from `wrangler.toml` [vars] for plain
# strings, and from `wrangler secret put` for secrets. This file is
# a copy-paste reference for what the worker consults at runtime,
# committed to the repo so contributors have one place to look.
#
# Per docs/specs/002-project-scope/secrets-policy.md the repo is public, so never put real secret values
# in this file: hex / token shapes are placeholders only.

# Clerk JWKS endpoint, used to verify Bearer tokens for the
# authed-owner path (docs/specs/014-identity/auth-and-guest-access.md). Unset = pure-guest mode; the worker
# still serves on X-Owner-Id alone. Drop into .dev.vars for local
# dev; provision via `wrangler secret put CLERK_JWKS_URL` in
# production.
#
# Teams (docs/specs/013-workspace/teams.md) additionally reads an optional `email` claim from
# the verified token to auto-connect pending invites. Add it via the
# Clerk dashboard's session-token customisation:
#   { "email": "{{user.primary_email_address}}" }
# No env var needed — it travels inside the JWT.
# CLERK_JWKS_URL=https://clerk-instance.example/.well-known/jwks.json

# Optional: when set, the Clerk verifier also asserts the token's `iss`
# claim, so a validly-signed token from a different Clerk instance that
# happens to share the JWKS host can't be replayed. Unset = issuer not
# asserted (back-compat). Set it to your instance's issuer URL.
# CLERK_ISSUER=https://clerk-instance.example

# Optional: when set, the Clerk verifier also asserts the token's `aud`
# claim, so a token minted for a different audience/app can't be replayed
# here. Unset = audience not asserted (back-compat). Set it to the audience
# you configure in Clerk for this API.
# CLERK_AUDIENCE=livediagram-api

# HMAC secret for signing guest owner-ids (docs/specs/014-identity/auth-and-guest-access.md). When set, the worker
# mints signed guest ids (POST /api/guest-id) and /api/migrate requires a
# valid signature before reassigning a guest's data to a Clerk account —
# so observing a guest's id (which leaks via DTOs / presence) is not enough
# to claim its data. Strongly recommended whenever Clerk is configured.
# Unset = legacy unsigned migrate (acceptable for a single-user self-host).
# Use a long random value: `openssl rand -hex 32`. Provision like
# CLERK_JWKS_URL (.dev.vars locally, `wrangler secret put` in prod).
# GUEST_ID_HMAC_SECRET=replace-with-openssl-rand-hex-32

# Guest REST signature enforcement cutoff (docs/specs/015-api/public-api-and-tokens.md §4), epoch ms. While
# unset (or set to a future time) the guest `X-Owner-Id` REST path keeps
# its legacy unsigned behaviour: the grace window that lets pre-signing
# guests self-heal to a signed id. Once active guests have rotated, set it
# to a past epoch-ms timestamp to require a valid `X-Owner-Sig` on
# owner-scoped routes. No-op without GUEST_ID_HMAC_SECRET. A plain
# wrangler.toml [vars] string (not a secret).
# GUEST_SIG_ENFORCE_AFTER=1700000000000

# Anonymous telemetry ingest + dashboard (docs/specs/017-telemetry/telemetry.md). Only the literal
# string "true" enables both. Unset / "false" / anything else keeps
# the events route accepting and dropping POSTs (204 with no D1
# write) and the dashboard route returning empty totals, so OSS
# self-host stays silent by default.
# TELEMETRY_ENABLED=true

# Shared secret marking a /api/events post as coming from one of our own
# workers over a service binding rather than the open internet (docs/specs/017-telemetry/telemetry.md).
# Those calls carry no CF-Connecting-IP, so without this they all collapse
# onto one 'anonymous' per-IP rate-limit key and throttle each other --
# which was silently dropping MCP telemetry. Set the SAME value on the mcp
# worker (`wrangler secret put INTERNAL_EVENTS_KEY` in both). Unset on
# either side = nobody is exempt, which is safe and needs no config, so a
# self-host can ignore this entirely.
# INTERNAL_EVENTS_KEY=<random string>

# OpenAI integration for the AI assistance feature (docs/specs/007-editor/ai-assistance.md).
# AI (docs/specs/007-editor/ai-assistance.md). The PROVIDER is inferred from which key you set — a key is
# provider-specific, so its name says whose it is. Set exactly ONE; two of them
# is refused rather than guessed at. No key = the whole AI surface hides
# (capabilities reports aiEnabled:false, the routes answer 503).
#
# Google AI Studio (what the hosted site uses):
# GOOGLE_AI_STUDIO_API_KEY=...
#
# OpenAI:
# OPENAI_API_KEY=sk-...
#
# Anything else that speaks the OpenAI wire (Mistral, OpenRouter, a local
# llama.cpp or Ollama) — needs the base URL and the model as well:
# AI_API_KEY=not-used-but-required
# AI_BASE_URL=http://127.0.0.1:8080/v1
# AI_MODEL=whatever-you-loaded
#
# Optional for any provider:
# AI_MODEL=...          overrides the preset default (gemini-3.6-flash / gpt-4o)
# AI_VISION_MODEL=...   the crop reader only (docs/specs/021-event-storming/event-storming.md Phase 8). On Google this
#                       defaults to gemini-2.5-flash-lite, which reads handwriting
#                       better than the assistant model and costs less.

# Optional spend-DoS defences on /api/ai (docs/specs/007-editor/ai-assistance.md). Both default to
# "off" so OSS self-hosters keep the historical open behaviour.
# Hosted livediagram.app sets both.
# AI_ALLOWED_ORIGINS=https://livediagram.app,http://localhost:3002
# AI_REQUIRE_CLERK=true

# Optional per-owner soft caps on the image gallery (docs/specs/009-elements/images.md). Both
# decimal strings; unset / 0 / non-numeric = no limit. Hosted
# livediagram.app sets these to 100 and 104857600 (100 MB) so one
# owner can't drain the R2 bucket. POST /api/images returns 403
# { error: "gallery_full" } when either cap is hit.
# IMAGE_MAX_PER_OWNER=100
# IMAGE_MAX_BYTES_PER_OWNER=104857600

# Optional transactional + lifecycle email via Resend (docs/specs/014-identity/transactional-email.md). An
# absent RESEND_API_KEY means no email is ever sent and the
# email_lifecycle table is never touched. RESEND_FROM + APP_BASE_URL
# are optional [vars] (defaults: "livediagram <hello@livediagram.app>"
# and "https://livediagram.app"). The From domain must be verified in
# Resend.
# RESEND_API_KEY=re_...
# RESEND_FROM=livediagram <hello@livediagram.app>
# APP_BASE_URL=https://livediagram.app
