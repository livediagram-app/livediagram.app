// The 9px share-state badge glyphs (spec/24 / spec/35) now live in
// @livediagram/ui, shared with the marketing hero illustration. This
// re-export stays only for the Explorer's shared-diagram row badge, which
// still imports from here; new code imports from @livediagram/ui.
export { PrivateDotIcon, SharedDotIcon } from '@livediagram/ui';
