import { ArticleLayout } from '@/components/ArticleLayout';
import { getSubArticles } from '@/lib/articles';
import { helpMetadata } from '@/lib/seo';
import {
  ToolbarLayoutOverview,
  ToolbarMorePopover,
} from '@/components/illustrations/toolbar-layout';

export const metadata = helpMetadata({
  title: 'Toolbar Layout',
  description:
    'Put the palette in one strip across the top of the canvas, with a menu button for the Explorer. The rest of the panels float as usual.',
  path: '/help/palette/toolbar-layout/',
});

<ArticleLayout
  title="Toolbar Layout"
  description="Put the palette in one strip across the top of the canvas, and open the Explorer from a menu button."
  categoryTitle="Palette"
  categorySlug="palette"
  subArticles={getSubArticles("toolbar-layout")}
>

Toolbar is one of three choices in the **Panel Layout** setting (Floating, Minimal, Toolbar). You will find it in [Settings](/help/user-interface/settings/), under **Appearance**, where a small picture of each layout shows how they differ before you switch. Floating is the default.

## What it does

The palette becomes a single strip across the top of the canvas. From left to right it holds:

- **Selection mode**: Select, Hand, Eraser, Laser and the rest, the same choices as the palette's own picker.
- **Category**: which set of tiles the strip shows, such as Favourites, Shapes or Icons.
- **Ten tiles** from that category. Click one to add it, or drag it onto the canvas, exactly as from the palette. Each tile shows its keyboard shortcut in the corner.
- **More** (the **⋯** button), when the category has more than the strip can hold.

There is no floating Explorer panel. The **menu button** in the top-left corner opens the Explorer under it, and clicking it again closes it. **Layers** and **Tab Activity** open just above their buttons in the bottom-right corner, and close when you click the button again. Every other panel (Collaborate and the rest) floats and docks exactly as it does in the Floating layout.

<Figure caption="The Toolbar layout: the menu button top-left, the strip across the top, and the usual bottom row.">
  <ToolbarLayoutOverview />
</Figure>

## More

**More** opens the whole category under the button: its search box and every tile. Adding a tile from it closes it again, so the canvas is clear to draw on.

## Tiles you use come first

The strip orders each category by what you use. Add something, from the strip or from **More**, and it moves to the first slot on the strip, pushing the others along one. The last tile drops off the end and waits in **More** until you use it again. So if you add a Timer from **More**, the Timer takes the first slot and whichever tile was tenth moves into **More**.

The order is remembered in this browser. It only applies to the Toolbar layout: the floating palette keeps your Favourites in the order you set. That's also why Favourites in **More** has no Edit or Reorder buttons here. To choose which tiles are your Favourites, use the star beside a search result, or switch to the Floating layout and use **Edit**.

<Figure caption="More open on Shapes, with the full category hanging under the button.">
  <ToolbarMorePopover />
</Figure>

The strip keeps the category you chose for as long as the page is open, including through Zen mode. Opening a diagram starts on Favourites again, like the floating palette.

## On a phone

Toolbar works on a phone too. The strip is narrower there:

- The **menu button** sits at the left end of the strip rather than in the corner.
- The **category** shows its icon only, which leaves room for more tiles. How many tiles fit depends on how wide your screen is; **More** has the rest, and opens across the width of the screen.
- **Layers** and **Tab Activity** open above their buttons in the bottom-right corner, as on a computer. Pinch to zoom; the **Fit** button is there too.

Floating is the one layout a phone can't use. If you pick it, a phone shows the compact button bar from [Minimal Panels](/help/palette/minimal-panels/) instead.

</ArticleLayout>
