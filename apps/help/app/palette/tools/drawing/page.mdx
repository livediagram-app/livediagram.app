import { ArticleLayout } from '@/components/ArticleLayout';
import { getSubArticles } from '@/lib/articles';
import { helpMetadata } from '@/lib/seo';
import { PencilStroke, ShapeRecognition } from '@/components/illustrations/palette-tools';
import { HighlighterStroke, PolygonDraw } from '@/components/illustrations/palette-tools-drawing';

export const metadata = helpMetadata({
  title: 'Drawing and Sketch',
  description:
    'Sketch with the Freehand pen, snap rough sketches to real shapes with the Shape Pen, call attention to things with the Highlighter, and place clean point-by-point outlines with the Polygon tool.',
  path: '/help/palette/tools/drawing/',
});

<ArticleLayout
  title="Drawing and Sketch"
  description="Sketch with the Freehand pen, snap rough sketches into real shapes with the Shape Pen, highlight with the marker, and place point-by-point polygons."
  categoryTitle="Palette"
  categorySlug="palette"
  parentArticle={{ title: "Tools", slug: "tools" }}
  subArticles={getSubArticles("drawing")}
>

Sometimes the fastest way to capture an idea is to draw it. The palette's **Draw** category holds four drawing tools: **Freehand** for smooth strokes kept exactly as drawn, the **Shape Pen** for rough sketches that snap to real shapes, the **Highlighter** for calling attention to things with a wide translucent marker, and the **Polygon** tool for clean point-by-point outlines. Everything they draw is a regular element you can move, resize, and style.

## Freehand and the Shape Pen

Both pens are gestural tools: pick one from the palette's **Draw** category (**Freehand** is `P`) and the next drag on the canvas draws a stroke. A small mode banner tells you which pen is armed, with a Cancel action, and the cursor swaps to a pen icon so you know you are in draw mode.

The only difference is what happens on release: Freehand keeps the stroke exactly as you drew it, and the Shape Pen matches it against real shapes.

<Step n={1}>Select Freehand or the Shape Pen from the palette's Draw category.</Step>
<Step n={2}>Drag across the canvas to draw. A live preview follows your pointer.</Step>
<Step n={3}>
  Release to commit the stroke. A stray click with no drag does not create anything.
</Step>

On release, your raw points are smoothed into a clean, hand-drawn curve rather than a jagged polyline. If you release near where you started, the path closes into a custom shape and fills, which is the way to sketch a bespoke outline.

<Figure caption="Freehand in draw mode, with a smoothed stroke and the Drag to draw banner.">
  <PencilStroke />
</Figure>

## Styling a sketch

A committed sketch is a regular boxed element. Its stroke colour follows the tab's theme so it reads as part of the diagram rather than a separate annotation layer, and a closed path fills with the theme's fill colour. From there you can:

- Move it, resize it (the path scales proportionally), and lock or group it.
- Recolour its stroke and fill from the element's Colours controls.
- Apply the format painter, themes, and comments, exactly as with any shape.

## The Highlighter

The Highlighter sits directly beside the two pens in the Draw category and works the same way: pick it, then drag. Instead of a pen line it lays down a **wide, semi-transparent marker stroke**, so text and shapes underneath stay legible and overlapping strokes darken like a real marker. It is made for reviews and workshops, circling a region or underlining a label, rather than for sketching shapes.

<Figure caption="A highlighter swipe over a shape: the wide translucent stroke leaves the label underneath legible.">
  <HighlighterStroke />
</Figure>

A few deliberate differences from the two pens:

- **Always yellow on creation**, whatever the theme, so highlights read as annotations rather than diagram content. You can recolour a committed stroke afterwards from its Colours controls, like any element.
- **No shape recognition.** Highlighter strokes always commit exactly as drawn. Recognition is the Shape Pen's job.
- **No close-to-fill.** Ending a stroke back at its start never closes or fills it; a highlight is always an open stroke.

The marker keeps its width when you resize the stroke, so a scaled-up highlight still looks like the same pen.

## The Polygon tool

The Polygon tool fills the gap between freehand sketching and the fixed shapes: clean, straight-edged outlines with exactly the corners you choose, for zones on an architecture diagram, floor plans, or any irregular region.

<Step n={1}>Select the Polygon tool from the Draw category.</Step>
<Step n={2}>
  Click to place each point. A preview line follows your pointer from the last point you placed.
</Step>
<Step n={3}>
  Click the **start point** (a snap ring appears when you are close) to close the outline into a
  **filled polygon**, or **double-click** (or press `Enter`) to finish an **open line** at the last
  point.
</Step>

<Figure caption="Placing polygon points click by click, with the snap ring on the start point ready to close the outline.">
  <PolygonDraw />
</Figure>

While placing points, `Backspace` removes the last point so you can walk a mistake back without starting over, and `Escape` clears the in-progress outline. A closed polygon fills with the theme's fill colour like any closed sketch; an open line stays stroke-only.

## Shape recognition

Pick the **Shape Pen** instead of Freehand and your sketches are matched against real shapes on commit. A rough square becomes a square, a loop becomes a circle, and a straight stroke becomes an arrow. See [Shape recognition](/help/palette/tools/drawing/shape-recognition/) for the details and which kinds are detected.

<Figure caption="Drawn with the Shape Pen, a rough sketch snaps into a clean shape on commit.">
  <ShapeRecognition />
</Figure>

<Tip>
  Nothing is remembered between strokes: Freehand always keeps what you drew and the Shape Pen
  always tries to convert it, so a stroke never behaves differently from the last one for reasons
  you cannot see.
</Tip>

</ArticleLayout>
