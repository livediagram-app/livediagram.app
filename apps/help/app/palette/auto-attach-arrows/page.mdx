import { ArticleLayout } from '@/components/ArticleLayout';
import { getSubArticles } from '@/lib/articles';
import { helpMetadata } from '@/lib/seo';
import { PaletteSettings, AutoAttachArrows } from '@/components/illustrations/palette-modes';

export const metadata = helpMetadata({
  title: 'Auto-Attach Arrows',
  description:
    'Keep arrows pinned to the nearest face of a shape as it moves, so connectors always point at the right spot.',
  path: '/help/palette/auto-attach-arrows/',
});

<ArticleLayout
  title="Auto-Attach Arrows"
  description="Re-pin arrows to the nearest face of a shape automatically as that shape moves."
  categoryTitle="Palette"
  categorySlug="palette"
  subArticles={getSubArticles("auto-attach-arrows")}
>

Auto-Attach Arrows is a toggle, and it is off by default: arrows stay pinned exactly where you drew them until you opt in. You will find it behind the gear (settings) icon in the header of the floating palette, which opens a small settings popover.

<Figure caption="The palette settings popover, with Auto-attach arrows highlighted and switched on.">
  <PaletteSettings highlight="auto-attach" />
</Figure>

## What it does

When the setting is on, arrows re-pin to the nearest face or edge of a shape as that shape moves. The connector follows along, so it keeps pointing at the right spot without you having to tidy up endpoints by hand. In the settings popover the hint reads: "Re-pin arrows to the nearest face as shapes move."

Re-pinning is deliberately conservative: an arrow only picks a new face when its current one has genuinely stopped making sense, such as the shape being dragged to the other side of its partner. Nudging a connected shape a little never reshuffles a layout you have already arranged.

<Figure caption="As the target shape moves, the arrow re-pins to its nearest face instead of dangling at the old spot.">
  <AutoAttachArrows />
</Figure>

## Many arrows, one point

When several arrows pin to the same spot on a shape, say a hub with every child pointing at its bottom edge, the arrowheads fan out side by side along that edge instead of piling onto one point. The fan is purely visual and automatic: the pins themselves don't change, the heads just stay readable however many connectors converge.

## When to turn it on

Switch it on if you move shapes around a lot and want connectors to keep pointing at the nearest face without tidying endpoints by hand. Leave it off (the default) when you have positioned connectors deliberately and want every endpoint to hold exactly where you put it as shapes are dragged.

</ArticleLayout>
