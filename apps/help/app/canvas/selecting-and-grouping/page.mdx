import { ArticleLayout } from '@/components/ArticleLayout';
import { getSubArticles } from '@/lib/articles';
import { helpMetadata } from '@/lib/seo';
import { MultiSelect, FormatPainter } from '@/components/illustrations/canvas';

export const metadata = helpMetadata({
  title: 'Selecting Many Elements',
  description:
    "Select many elements at once to move, copy, lock or restyle them together, and copy one element's look onto others with the format painter.",
  path: '/help/canvas/selecting-and-grouping/',
});

<ArticleLayout
  title="Selecting Many Elements"
  description="Work with many elements at once: marquee and multi-select, and the format painter for copying a look."
  categoryTitle="Canvas"
  categorySlug="canvas"
  subArticles={getSubArticles("selecting-and-grouping")}
>

Diagrams rarely come together one shape at a time. Once a few elements are on the canvas you will want to move several at once, keep related pieces together, and make a row of shapes share the same look. livediagram gives you the tools for exactly that: selecting many elements, frames, and the format painter. None of them need a sign-in, so you can [open a canvas](/new) and try them straight away.

## Selecting more than one

The quickest way to grab several elements is the marquee: drag an empty patch of canvas and a selection rectangle picks up every element that sits entirely inside it. You can also build a selection click by click, adding and removing elements as you go. Either way, the selection becomes a single thing you can move, copy, or restyle in one go. See [Marquee and multi-select](/help/canvas/selecting-and-grouping/multi-select/) for every way to gather a selection.

<Figure caption="Drag a marquee across empty canvas to gather every fully enclosed element into one selection.">
  <MultiSelect />
</Figure>

## Keeping elements together

A selection scatters as soon as you click away. When some elements should keep travelling together, draw a [frame](/help/palette/build/frames/) around them: moving the frame carries everything inside it. And when the thing you are building is really one object, such as a banner, a KPI row or a website header, reach for a [component](/help/palette/components/), which is a single element rather than pieces you have to hold together.

## Copying a look

Once you have styled one element just right, the [format painter](/help/selection-modes/format-painter/) copies that look onto others in a couple of clicks, so a whole diagram can share one consistent style without re-setting each colour and border by hand.

<Figure caption="The format painter picks up one element's look and brushes it onto another.">
  <FormatPainter />
</Figure>

<Tip>
  Marquee selection pairs well with the format painter: select a row of shapes, then paint a styled
  element's look across all of them at once.
</Tip>

</ArticleLayout>
