import { ArticleLayout } from '@/components/ArticleLayout';
import { helpMetadata } from '@/lib/seo';
import { MobileEditor, TouchGestures } from '@/components/illustrations/devices';

export const metadata = helpMetadata({
  title: 'Mobile',
  description:
    'livediagram on a phone is a touch-friendly editor: pinch to zoom, drag to pan, long-press for menus, and the palette as one strip across the top.',
  path: '/help/supported-devices/mobile/',
});

<ArticleLayout title="Mobile" description="On a phone the editor is built for touch, with a palette strip across the top and finger gestures in place of a mouse and keyboard." categoryTitle="Supported Devices" categorySlug="supported-devices">

livediagram works on a phone right in your browser, with no app to install. The editor automatically switches to a touch-friendly layout on small screens so the canvas stays the star and everything is reachable with a thumb. It is great for viewing a diagram, making quick edits, and joining a live session on the go.

## A layout built for touch

On a small screen the editor starts in the [Toolbar layout](/help/palette/toolbar-layout/): the palette becomes one strip across the top, with a menu button at its left end for the Explorer, and **More** for everything that doesn't fit. Prefer a **compact dock** instead? Pick **Minimal** under **Panel Layout** in [Settings](/help/user-interface/settings/): the panels then open from a row of buttons in the top corner, each as a pop-over that tidies itself away after you add something.

The editor also opens zoomed out a little so you land on a useful overview of the whole diagram, and when you add a new element it slides into view automatically.

<Figure caption="On a phone the palette becomes one strip across the top, with the menu button at its left end, and the diagram is zoomed to fit, keeping the canvas front and centre.">
  <MobileEditor />
</Figure>

## Touch gestures

Your fingers do the work a mouse would on a computer:

- **Pinch** with two fingers to zoom in and out.
- **Drag** to move around the canvas.
- **Tap** to select, and **drag** an element to move it.
- **Press and hold** an element (a long press) to open its menu, the touch version of a right-click.

Buttons and drag handles have a little extra invisible padding on touch, so they are easier to hit accurately with a fingertip.

<Figure caption="Your fingers do the work a mouse would: pinch to zoom, drag to pan, tap to select, and press and hold for the menu.">
  <TouchGestures />
</Figure>

## What works differently on a phone

A phone has no mouse pointer or physical keyboard, so a few things made for those are not available on a small screen:

- **Keyboard shortcuts** are off, since there are no keys to press. Use the palette and the on-screen controls instead, which do everything the shortcuts do.
- **Shortcuts that need a held key**, like Shift to keep proportions while resizing or to add to a selection, are not available without a keyboard.
- **Spotlight** (the presenting highlight) and **dragging a box to multi-select** rely on a hovering, clicking mouse, so they are reserved for larger screens.

<Tip>
  For a long or detailed editing session, a [tablet](/help/supported-devices/tablet/) with more room
  (or a [computer](/help/supported-devices/desktop/)) is more comfortable. A phone is perfect for
  quick changes and following along live.
</Tip>

</ArticleLayout>
