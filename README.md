# PR screenshots

An orphan branch holding the images referenced from pull-request descriptions,
so `main` never carries screenshot binaries. Nothing here is built, deployed or
imported: GitHub serves the files raw, and the branch can be deleted once the
PR that references it is closed.
